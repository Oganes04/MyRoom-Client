import { Typography } from "@mui/material";
import { FC } from "react";

interface Props {
  onClick: () => void;
}

const AddButton: FC<Props> = () => {
  return <Typography>12</Typography>;
};

export default AddButton;
